* DOM stands for document object modal.
* This is the representation of all HTML code in the Javascript object form generated by the browser.

* Window is the global object provided by the browser which contains all information about the tab in which the page is loaded. Even the DOM is the property of window object with key document.

* We can manipulate DOM using Javascript and the browser reads that DOM to render the page.

* We can access HTML elements by using the methods given by window object.
- getElementById(): Access HTML element by id attribute.
- getElementsByClassName(): Returns array like structure called HTMLCollection having all elements of the same class attribute.
- getElementsByTagName(): Returns array like structure called HTMLCollection having all element of the specified element.
- querySelector(): Returns the first occurrence of the given CSS selector.
- querySelectorAll(): Returns array like structure called NodeList containing all elements of the given matching CSS selector.

* Text Properties of elements can be used to get or set the text or even entire HTML of an element.
- innerHTML can be used to get or set the HTML elements and text inside an element.
- outerHTML can be used to get or set the HTML elements and text inside an element including the element itself.
- innerText can be used to get or set the visible text of the element.
- textContent can be used to get or set the entire text in an element either visible or not.

* DOM Navigation
- children: children property return HTMLCollection of all HTML elements inside an element.
- childNodes: childNodes property returns NodeList of all Nodes inside an element including text, comment and element nodes.
- firstElementChild: Returns the first element of an element.
- firstChild: Returns the first Node of an element.
- lastElementChild: Returns the last element of an element.
- lastChild: Returns the last Node of an element.
- parentElement: Returns the parent element of an element.
- parentNode: Returns the parent Node of an element.
- nextElementSibling: Returns the next element of an element sharing the same parent.
- nextSibling: Returns the next Node of an element sharing the same parent.
- previousElementSibling: Returns the previous element of an element sharing the same parent.


- contains(): Returns Boolean value based on if a Node is present in another Node.
- closest(): Returns the nearest ancestor matching the specified CSS selector.